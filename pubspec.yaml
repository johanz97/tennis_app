name: tennis_app
description: "Test"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  dartz: ^0.10.1
  firebase_auth: ^5.1.2
  firebase_core: ^3.2.0 
  flutter:
    sdk: flutter
  go_router: ^14.2.1
  provider: ^6.1.2
  very_good_analysis: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
